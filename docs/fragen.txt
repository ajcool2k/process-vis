Anmerkungen Datenmodell:

- Booleans
	- true/false sollten boolean Values sein, keine String
	- Beispiel: transformation.decision

- Files
	- Wie ist die Struktur einer Datei im Datenmodell?
	- Beispiel: "files": []

- Leer-Werte
	- Wie Verfahren wir mit nicht gesetzten Attributen
	- Beispiel: this.parent
 	- Möglichkeiten: leer, null, nicht vorhanden?

- Optionale-Attribute
	- Wie Verfahrne wir mit optionalen Attributen
	- Beispiel: wie copyright

- Zulässige Werte
	- einige Felder haben noch keine vordefinierten Werten
	- Beispiel: contactPerson (String/Stakeholder)?, participation ('open' / 'closed')

- Werden Meta-Informationen im Head-Prozess gespeichert?
	- Beispiel: this.locations, stakeholder, ..

- Für stellt die Angabe pariticpated in der Entität Stakeholder dar?
	- List aus ProzessIDs?

Weitere:

- Validator würde ich gern verwenden, wenn vorhanden


Erkenntnisse:

- locations, stakeholder und location auf top-level oder als meta-infomation
- wird nicht im prozess in der datenbank gespeichert, sondern als eigenständiges Document